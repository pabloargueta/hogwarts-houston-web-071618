import React, { Component } from 'react'
import hogData from '../porkers_data.js'

const Filter = (props) => {



  return (
    <div>
      <button onClick={this.props.setState({})}>test</button>
    </div>
  )
}

export default Filter